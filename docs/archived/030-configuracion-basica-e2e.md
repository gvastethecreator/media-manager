# [030] Configuración básica de pruebas E2E

Se configuró Playwright para utilizar el puerto 5173 y se sincronizó el archivo
`playwright-mcp.config.json`. Se añadió la prueba inicial `tests/e2e/basic.spec.ts`
y se documentó en el README el flujo de instalación de navegadores y ejecución de
`pnpm test`.

**TAREA COMPLETADA**
