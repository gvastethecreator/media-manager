// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Folder {
  id            String    @id @default(cuid())
  name          String
  path          String    @unique
  totalFiles    Int       @default(0)
  totalSize     Int       @default(0)
  lastIndexed   DateTime? @default(now())
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  autoReindex   Boolean   @default(false)
  images        Image[]
  featuredImage String?
  isFavorite    Boolean   @default(false)

  @@index([path])
  @@index([lastIndexed])
  @@index([createdAt])
}

model Image {
  id                   String       @id @default(cuid())
  hash                 String
  name                 String
  path                 String
  size                 Int
  width                Int
  height               Int
  metadata             String?
  thumbnail            Bytes?
  thumbnailSize        Int?
  thumbnailWidth       Int?
  thumbnailHeight      Int?
  thumbnailError       String?
  thumbnailErrorAt     DateTime?
  thumbnailOptimizedAt DateTime?
  isPublic             Boolean      @default(false)
  isFavorite           Boolean      @default(false)
  folderId             String
  folder               Folder       @relation(fields: [folderId], references: [id], onDelete: Cascade)
  createdAt            DateTime     @default(now())
  updatedAt            DateTime     @updatedAt
  collections          Collection[] @relation("ImageToCollection")
  tags                 Tag[]        @relation("ImageToTag")
  favorites            Favorite[]
  stats                ImageStats?
  activities           Activity[]
  albums               Album[]      @relation("ImageToAlbum")
  characters           Character[]  @relation("ImageToCharacter")
  places               Place[]      @relation("ImageToPlace")
  objects              Object[]     @relation("ImageToObject")

  @@unique([path, folderId], name: "path_folderId")
  @@index([folderId])
  @@index([hash])
  @@index([createdAt])
  @@index([updatedAt])
  @@index([isPublic])
  @@index([isFavorite])
}

model Tag {
  id            String   @id @default(cuid())
  name          String   @unique
  emoji         String   @default("🏷️")
  color         String   @default("#3b82f6")
  description   String?
  shortcut      String?
  featuredImage String?
  isFavorite    Boolean  @default(false)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  images        Image[]  @relation("ImageToTag")

  @@index([name])
  @@index([createdAt])
}

model Collection {
  id             String   @id @default(cuid())
  name           String   @unique
  emoji          String   @default("🌟")
  description    String?
  color          String   @default("#3b82f6")
  shortcut       String?
  sortBy         String   @default("name")
  filters        String   @default("[]")
  url            String?
  alternativeUrl String?
  sourceImage    String?
  platform       String?
  price          Float?
  editions       String   @default("[]")
  featuredImage  String?
  isFavorite     Boolean  @default(false)
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
  images         Image[]  @relation("ImageToCollection")

  @@index([name])
  @@index([createdAt])
}

model Favorite {
  id        String   @id @default(cuid())
  imageId   String
  image     Image    @relation(fields: [imageId], references: [id])
  createdAt DateTime @default(now())

  @@index([imageId])
}

model ImageStats {
  id         String   @id @default(cuid())
  imageId    String   @unique
  image      Image    @relation(fields: [imageId], references: [id])
  views      Int      @default(0)
  downloads  Int      @default(0)
  lastViewed DateTime @default(now())
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  @@index([imageId])
}

model Profile {
  id          String   @id @default(cuid())
  name        String
  emoji       String   @default("👤")
  color       String   @default("#3b82f6")
  theme       String   @default("system")
  language    String   @default("es")
  description String?
  isActive    Boolean  @default(false)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model Album {
  id          String   @id @default(cuid())
  name        String   @unique
  emoji       String   @default("📸")
  description String?
  color       String   @default("#3b82f6")
  shortcut    String?
  sortBy      String   @default("name")
  filters     String   @default("[]")
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  images      Image[]  @relation("ImageToAlbum")

  @@index([name])
  @@index([createdAt])
}

model Character {
  id                   String      @id @default(cuid())
  name                 String      @unique
  emoji                String      @default("👤")
  color                String      @default("#3b82f6")
  description          String?
  shortcut             String?
  level                Int         @default(1)
  class                String      @default("unknown")
  race                 String      @default("unknown")
  alignment            String      @default("neutral")
  backstory            String      @default("")
  stats                String      @default("{}")
  sortBy               String      @default("name")
  filters              String      @default("[]")
  psychologicalProfile String      @default("")
  socialProfile        String      @default("")
  relationships        String      @default("[]")
  goals                String      @default("[]")
  fears                String      @default("[]")
  beliefs              String      @default("[]")
  personality          String      @default("[]")
  featuredImage        String?
  isFavorite           Boolean     @default(false)
  createdAt            DateTime    @default(now())
  updatedAt            DateTime    @updatedAt
  images               Image[]     @relation("ImageToCharacter")
  relatedCharacters    Character[] @relation("CharacterRelations")
  relatedTo            Character[] @relation("CharacterRelations")
  notes                Note[]      @relation("CharacterToNote")
  concepts             Concept[]   @relation("CharacterToConcept")
  prompts              Prompt[]    @relation("CharacterToPrompt")
  attributes           Attribute[] @relation("CharacterToAttribute")

  @@index([name])
  @@index([createdAt])
  @@index([class])
  @@index([race])
}

model Place {
  id            String      @id @default(cuid())
  name          String      @unique
  emoji         String      @default("📍")
  color         String      @default("#3b82f6")
  description   String?
  shortcut      String?
  region        String      @default("unknown")
  type          String      @default("unknown")
  climate       String      @default("temperate")
  population    Int         @default(0)
  government    String      @default("unknown")
  dangers       String      @default("[]")
  resources     String      @default("[]")
  lore          String      @default("")
  history       String      @default("")
  stats         String      @default("{}")
  sortBy        String      @default("name")
  filters       String      @default("[]")
  featuredImage String?
  isFavorite    Boolean     @default(false)
  notes         Note[]      @relation("PlaceToNote")
  concepts      Concept[]   @relation("PlaceToConcept")
  prompts       Prompt[]    @relation("PlaceToPrompt")
  attributes    Attribute[] @relation("PlaceToAttribute")
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt
  images        Image[]     @relation("ImageToPlace")

  @@index([name])
  @@index([createdAt])
  @@index([type])
  @@index([climate])
  @@index([region])
}

model Object {
  id            String      @id @default(cuid())
  name          String      @unique
  emoji         String      @default("🎯")
  color         String      @default("#3b82f6")
  description   String?
  shortcut      String?
  type          String      @default("misc")
  rarity        String      @default("common")
  properties    String      @default("[]")
  requirements  String      @default("{}")
  origin        String      @default("")
  stats         String      @default("{}")
  sortBy        String      @default("name")
  filters       String      @default("[]")
  featuredImage String?
  isFavorite    Boolean     @default(false)
  notes         Note[]      @relation("ObjectToNote")
  concepts      Concept[]   @relation("ObjectToConcept")
  prompts       Prompt[]    @relation("ObjectToPrompt")
  attributes    Attribute[] @relation("ObjectToAttribute")
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt
  images        Image[]     @relation("ImageToObject")

  @@index([name])
  @@index([createdAt])
  @@index([type])
  @@index([rarity])
}

// Modelo para el sistema de colas
model QueueJob {
  id          String    @id @default(cuid())
  queue       String
  data        String
  status      String    @default("pending")
  attempts    Int       @default(0)
  maxAttempts Int       @default(3)
  error       String?
  progress    Int       @default(0)
  startedAt   DateTime?
  finishedAt  DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  priority    Int       @default(0)
  metadata    String?
  retryAt     DateTime?

  @@index([queue, status])
  @@index([status, createdAt])
  @@index([priority, status, createdAt])
  @@index([retryAt])
}

model Activity {
  id          String   @id @default(cuid())
  type        String
  description String
  imageId     String?
  image       Image?   @relation(fields: [imageId], references: [id])
  createdAt   DateTime @default(now())

  @@index([createdAt])
  @@index([imageId])
}

// Nuevos modelos

model Concept {
  id            String      @id @default(cuid())
  name          String      @unique
  emoji         String      @default("💡")
  color         String      @default("#3b82f6")
  description   String?
  content       String      @default("")
  category      String      @default("general")
  tags          String      @default("[]")
  featuredImage String?
  isFavorite    Boolean     @default(false)
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt
  characters    Character[] @relation("CharacterToConcept")
  places        Place[]     @relation("PlaceToConcept")
  objects       Object[]    @relation("ObjectToConcept")
  notes         Note[]      @relation("ConceptToNote")
  prompts       Prompt[]    @relation("ConceptToPrompt")
  attributes    Attribute[] @relation("ConceptToAttribute")

  @@index([name])
  @@index([category])
  @@index([createdAt])
}

model Prompt {
  id            String      @id @default(cuid())
  name          String      @unique
  emoji         String      @default("🎯")
  color         String      @default("#3b82f6")
  description   String?
  content       String      @default("")
  category      String      @default("general")
  parameters    String      @default("{}")
  tags          String      @default("[]")
  featuredImage String?
  isFavorite    Boolean     @default(false)
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt
  characters    Character[] @relation("CharacterToPrompt")
  places        Place[]     @relation("PlaceToPrompt")
  objects       Object[]    @relation("ObjectToPrompt")
  concepts      Concept[]   @relation("ConceptToPrompt")
  notes         Note[]      @relation("PromptToNote")
  attributes    Attribute[] @relation("PromptToAttribute")

  @@index([name])
  @@index([category])
  @@index([createdAt])
}

model Note {
  id            String      @id @default(cuid())
  title         String
  content       String      @default("")
  category      String      @default("general")
  priority      Int         @default(0)
  status        String      @default("active")
  tags          String      @default("[]")
  featuredImage String?
  isFavorite    Boolean     @default(false)
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt
  characters    Character[] @relation("CharacterToNote")
  places        Place[]     @relation("PlaceToNote")
  objects       Object[]    @relation("ObjectToNote")
  concepts      Concept[]   @relation("ConceptToNote")
  prompts       Prompt[]    @relation("PromptToNote")
  attributes    Attribute[] @relation("NoteToAttribute")

  @@index([title])
  @@index([category])
  @@index([priority])
  @@index([status])
  @@index([createdAt])
}

model Attribute {
  id            String      @id @default(cuid())
  name          String      @unique
  type          String      @default("text")
  value         String      @default("")
  category      String      @default("general")
  description   String?
  metadata      String      @default("{}")
  featuredImage String?
  isFavorite    Boolean     @default(false)
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt
  characters    Character[] @relation("CharacterToAttribute")
  places        Place[]     @relation("PlaceToAttribute")
  objects       Object[]    @relation("ObjectToAttribute")
  concepts      Concept[]   @relation("ConceptToAttribute")
  prompts       Prompt[]    @relation("PromptToAttribute")
  notes         Note[]      @relation("NoteToAttribute")

  @@index([name])
  @@index([type])
  @@index([category])
  @@index([createdAt])
}

// Sistema de Favoritos Universal
model UniversalFavorite {
  id         String   @id @default(cuid())
  entityId   String
  entityType String
  createdAt  DateTime @default(now())

  @@unique([entityId, entityType])
  @@index([entityType])
  @@index([createdAt])
}

// Sistema de Imágenes del Sistema
model SystemImage {
  id        String   @id @default(cuid())
  name      String
  path      String   @unique
  type      String   @default("icon")
  category  String   @default("system")
  size      Int
  width     Int
  height    Int
  metadata  String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([type])
  @@index([category])
  @@index([createdAt])
}
