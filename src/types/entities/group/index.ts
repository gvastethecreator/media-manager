/**
 * @file Índice para tipos de Group
 * @module types/entities/group
 *
 * ⚠️ Limpieza: Solo se exportan tipos canónicos desde './types'.
 * Legacy eliminado.
 */

export { GroupSchema } from './types';
export type {
	GroupBase,
	GroupCreateInput,
	GroupUpdateInput
} from './types';

// 📝 Documentación: Solo tipos canónicos. Legacy removido.
