/**
 * @file Sort types
 * @description Re-exports sort-related types
 */

// Re-export SortOption from the view options store (simpler version without label)
export type { SortOption } from '@/store/ui/view-options.slice';
