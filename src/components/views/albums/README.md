# Albums views

This directory contains the `AlbumsView` and `AlbumContentView` views that show and explore Albums.

```mermaid
flowchart TD
    AlbumsView --> AlbumCard
    AlbumsView --> AlbumContentView
```
