import { BasicSettingsCard } from '../common/basic-settings-card';

export function WorkflowSettings() {
	return (
		<BasicSettingsCard title="Workflows" description="Configuraciones y gestión de archivos de workflow (JSON)." />
	);
}
