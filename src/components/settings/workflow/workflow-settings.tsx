import { BasicSettingsCard } from '../common/basic-settings-card';

export function WorkflowSettings() {
	return (
		<BasicSettingsCard description="Configuraciones y gestión de archivos de workflow (JSON)." title="Workflows" />
	);
}
