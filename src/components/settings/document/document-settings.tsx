import { BasicSettingsCard } from '../common/basic-settings-card';

export function DocumentSettings() {
	return (
		<BasicSettingsCard title="Documentos" description="Configuraciones y gestión de documentos (markdown, txt)." />
	);
}
