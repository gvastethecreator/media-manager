import { BasicSettingsCard } from '../common/basic-settings-card';

export function DocumentSettings() {
	return (
		<BasicSettingsCard description="Configuraciones y gestión de documentos (markdown, txt)." title="Documentos" />
	);
}
