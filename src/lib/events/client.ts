/**
 * Archivo de compatibilidad para eventos del cliente
 * Re-exporta las funciones necesarias sin usar 'use client'
 */

import { clientEvents } from '../client/events.client';

// Re-exportar funciones y tipos
export { clientEvents };
