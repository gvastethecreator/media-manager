/**
 * Exportación de funciones de formateo comunes
 * @module lib/format
 */

import { formatDate as formatDateUtil } from './utils/format.utils';

// Exportación de funciones
export const formatDate = formatDateUtil;
