// Funciones principales de transformación para Workflow
import type { Workflow } from '@/types/entities/workflow';

export function normalizeWorkflow(workflow: Workflow): Workflow {
	// Aquí se pueden agregar normalizaciones adicionales
	return workflow;
}
