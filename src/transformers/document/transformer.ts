// Funciones principales de transformación para Document
import type { Document } from '@/types/entities/document';

export function normalizeDocument(doc: Document): Document {
	// Aquí se pueden agregar normalizaciones adicionales
	return doc;
}
