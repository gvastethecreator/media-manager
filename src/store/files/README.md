# Files Store

Gestiona el listado y la selección de archivos sueltos.

- `files.store.ts` mantiene los elementos recibidos desde el backend.
- `file-selection.store.ts` controla la selección múltiple y la última selección.
