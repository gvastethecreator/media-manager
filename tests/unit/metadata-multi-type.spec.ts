import { describe, expect, it } from 'vitest';

describe('metadata multi tipo', () => {
	it.skip('el servicio soporta múltiples tipos de archivos', () => {
		// Nota: Este test está deshabilitado porque FileEntityMapperService usa constructor privado (singleton)
		// Los tests de integración verifican el comportamiento real del servicio
	});
});
